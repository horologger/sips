  SIP: X
  Title: Appropriate .spaces top level DNS domain
  Author: horologger (horologger@protonmail.com)
  Status: Draft
  Type: Standards Track
  Created: 2025-09-24
  License: BSD-2-Clause
  Discussions-To: [TBD]

==Abstract==

Abstract -- A short (~200 word) description of the technical issue being addressed.

This document specifies a mechanism for translating ICANN DNS domain names into @spaces names.  The top-level domain name 'spaces' does not exist to date as a valid domain.  If a DNS proxy server would service a .spaces domain name as a @spaces name using the spaces protocol, clients that use DNS resolution would not need to be altered to perform spaces name resolution.

==Motivation==

Motivation -- The motivation should clearly explain why the existing protocol is inadequate to address the problem that the SIP solves.

The spaces protocol '@' delimiter confuses client applications that validate hostnames prior to passing them along for DNS resolution.

==Specification==

Specification -- The technical specification should describe the syntax and semantics of any new feature. The specification should be detailed enough to allow competing, interoperable implementations for any of the current Spaces platforms.

<syntaxhighlight lang="javascript">
function translateHostname(hostname) {
    // Force the hostname to lowercase
    hostname = hostname.toLowerCase();
    
    // Scan the hostname from right to left for the rightmost '.' and place it in top_string
    const lastDotIndex = hostname.lastIndexOf('.');
    if (lastDotIndex === -1) {
        throw new Error('Invalid hostname: no top-level domain found');
    }
    
    const topString = hostname.substring(lastDotIndex + 1);
    
    // if top_string == "spaces" then strip the rightmost '.spaces' and for the preceding '.' if exists
    if (topString === "spaces") {
        // Strip the rightmost '.spaces'
        const withoutSpaces = hostname.substring(0, lastDotIndex);
        
        // Check for preceding '.' if exists. If it doesn't exist then an error
        const secondLastDotIndex = withoutSpaces.lastIndexOf('.');
        if (secondLastDotIndex === -1) {
            throw new Error('Invalid spaces hostname: no subdomain found');
        }
        
        // Replace the '.' with '@' and process the resulting hostname as a spaces name
        const spacesName = withoutSpaces.substring(0, secondLastDotIndex) + '@' + withoutSpaces.substring(secondLastDotIndex + 1);
        return { type: 'spaces', name: spacesName };
    } else {
        // Pass it to the forwarding DNS server for resolution
        return { type: 'dns', name: hostname };
    }
}
</syntaxhighlight>


==Rationale==

Rationale -- The rationale fleshes out the specification by describing what motivated the design and why particular design decisions were made. It should describe alternate designs that were considered and related work.

==Backwards Compatibility==

Backwards compatibility -- All SIPs that introduce backward incompatibilities must include a section describing these incompatibilities and their severity. The SIP must explain how the author proposes to deal with these incompatibilities.

==Reference Implementation==

Reference implementation -- The reference implementation must be completed before any SIP is given the status "Final", but it need not be completed before the SIP is accepted.

==Copyright==

This SIP is licensed under the BSD-2-Clause License.
